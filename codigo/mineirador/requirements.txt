# =============================================================
#  Dependências do Minerador
#  Mantido separado para imagem mais leve (sem Streamlit/Neo4j)
# =============================================================

# HTTP — cliente para a API do GitHub
httpx==0.27.0

# Alternativa ao httpx (caso o grupo prefira)
# requests==2.31.0

# Validação e tipagem dos dados do contrato JSON
pydantic==2.6.4

# Variáveis de ambiente a partir do .env
python-dotenv==1.0.1