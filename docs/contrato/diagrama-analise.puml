@startuml diagrama-analise

title Bloco 4 — métricas sobre AbstractGraph (sem libs de grafo)

skinparam classAttributeIconSize 0
left to right direction

package "biblioteca/graph" as grafo {
  abstract class AbstractGraph {
    + hasEdge(u : int, v : int) : bool
    + getEdgeWeight(u : int, v : int) : float
    + getVertexOutDegree(u : int) : int
    + getVertexInDegree(u : int) : int
    + ...
  }
}

package "biblioteca/analysis" as analise {
  class GraphAnalyzer <<planned>> {
    __
    + degree_stats(g : AbstractGraph) : dict
    + density(g : AbstractGraph) : float
    + pagerank(g : AbstractGraph) : dict[int, float]
    + ...
  }
}

GraphAnalyzer ..> AbstractGraph : «use»

note bottom of GraphAnalyzer
  <<planned>> — sem NetworkX/igraph no TAD-base.
  NumPy opcional só como álgebra auxiliar.
end note

@enduml
