@startuml diagrama-contrato-interacoes

title Contrato mínimo — uma interação normalizada (1 linha JSON)

left to right direction

class InteractionRow <<DTO>> {
  + source_user : str
  + target_user : str
  + type : str
  + weight : int
  + repo : str
  + created_at : str
}

note right of InteractionRow
  **Regra**: ``source_user`` age → ``target_user`` recebe.
  **Integrado**: somar ``weight`` quando repetir o mesmo par orientado.
  **Tipos**: ``issue_comment``(2), ``pr_comment``(2),
  ``issue_close``(3), ``pr_open``(3), ``pr_review``(4), ``pr_merge``(5).
end note

@enduml
