@startuml diagrama-demo-aplicacao

title Demo / CLI — obrigação do enunciado (expor a API)

skinparam classAttributeIconSize 0
left to right direction

package "codigo/app" as demo {
  class DemoCLI <<planned>> {
    __
    + main() : void
    + cmdGraphSmoke() : void
    + cmdBuild() : void
    + cmdMine() : void
    + cmdMetrics() : void
  }
}

package "biblioteca/graph" as grafo {
  abstract class AbstractGraph
}

package "biblioteca/builder" as construtor {
  class GraphBuilder <<planned>>
}

package "codigo/mineirador" as mineirador {
  class GitHubMinerador
}

package "biblioteca/analysis" as analise {
  class GraphAnalyzer <<planned>>
}

DemoCLI ..> AbstractGraph : «use»
DemoCLI ..> GraphBuilder : «use» (opcional)
DemoCLI ..> GitHubMinerador : «use» (opcional)
DemoCLI ..> GraphAnalyzer : «use» (opcional)

note bottom of DemoCLI
  <<planned>> — demonstrar todas as operações públicas do grafo.
end note

@enduml
